'use client';
import { useEffect } from 'react';
export function Motion(){useEffect(()=>{const media=window.matchMedia('(prefers-reduced-motion: reduce)');let observer:IntersectionObserver|undefined;const nodes=Array.from(document.querySelectorAll<HTMLElement>('[data-reveal]'));function configure(){observer?.disconnect();nodes.forEach(n=>n.classList.remove('reveal-pending'));if(media.matches)return;observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.remove('reveal-pending');observer?.unobserve(entry.target);}})},{threshold:0.08});nodes.forEach(n=>{if(n.getBoundingClientRect().top>window.innerHeight){n.classList.add('reveal-pending');observer?.observe(n);}})}configure();media.addEventListener('change',configure);return()=>{observer?.disconnect();media.removeEventListener('change',configure);nodes.forEach(n=>n.classList.remove('reveal-pending'));}},[]);return null}
